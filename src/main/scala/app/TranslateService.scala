package app

class TranslateService(filename: String) {
  def run(): Unit = {
    // build RDD or DataFrame
    // combine reviews into strings up to 1000 symbols using special separator for further transformation
    // create ExecutorsService with 100 workers and schedule all jobs
  }
}
